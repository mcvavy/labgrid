{{- if .Values.hub.panel.httpRoute.enabled }}
{{- $g := .Values.gateway }}
{{- $sectionNames := default (list "http" "https") $g.sectionNames }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "tdai-memory.hub.fullname" . }}-panel
  labels:
    {{- include "tdai-memory.labels" . | nindent 4 }}
    app.kubernetes.io/component: hub-panel
spec:
  parentRefs:
{{- range $sectionNames }}
    - name: {{ $g.name | quote }}
      namespace: {{ $g.namespace | quote }}
      sectionName: {{ . | quote }}
{{- end }}
  hostnames:
{{- range .Values.hub.panel.httpRoute.hostnames }}
    - {{ . | quote }}
{{- end }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: {{ printf "%s-panel" (include "tdai-memory.hub.fullname" .) | quote }}
          port: {{ .Values.hub.panel.service.port }}
{{- end }}
