# Labgrid spike — install with release name "monitoring-spike" into namespace monitoring-spike.
# Service DNS names below assume: helm upgrade --install monitoring-spike ...

monitoringCredentials:
  enabled: true
  name: monitoring-credentials
  refreshInterval: "1h"
  target: labgrid-monitoring-credentials
  secretStoreRef:
    name: azure-kv-cluster-store
    kind: ClusterSecretStore
  data:
    - secretKey: grafana-admin-password
      remoteRef:
        key: labgrid-grafana-admin-password
    - secretKey: grafana-admin-user
      remoteRef:
        key: labgrid-grafana-admin-user

alloyEndpoints:
  clusterLabel: labgrid
  lokiPushUrl: http://monitoring-spike-loki-gateway.monitoring-spike.svc.cluster.local/loki/api/v1/push
  prometheusRemoteWriteUrl: http://kps-spike-prometheus.monitoring-spike.svc.cluster.local:9090/api/v1/write
  tempoOtlpEndpoint: http://monitoring-spike-tempo.monitoring-spike.svc.cluster.local:4317

kube-prometheus-stack:
  fullnameOverride: kps-spike

  grafana:
    enabled: false
    forceDeployDashboards: true

  defaultRules:
    create: true

  prometheus:
    prometheusSpec:
      replicas: 1
      retention: 15d
      scrapeInterval: 30s
      evaluationInterval: 30s
      enableRemoteWriteReceiver: true
      serviceMonitorSelector: {}
      podMonitorSelector: {}
      ruleSelector: {}
      resources:
        requests:
          cpu: 200m
          memory: 1Gi
        limits:
          cpu: 2000m
          memory: 4Gi
      storageSpec:
        volumeClaimTemplate:
          spec:
            storageClassName: synology-iscsi-delete-wffc
            accessModes:
              - ReadWriteOnce
            resources:
              requests:
                storage: 50Gi
    ingress:
      enabled: true
      ingressClassName: nginx
      hosts:
        - prometheus-spike.labgrid.net
      paths:
        - /
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-production
        nginx.ingress.kubernetes.io/backend-protocol: HTTP
        nginx.ingress.kubernetes.io/force-ssl-redirect: "true"

  alertmanager:
    alertmanagerSpec:
      replicas: 1
      retention: 120h
      resources:
        requests:
          cpu: 50m
          memory: 128Mi
        limits:
          cpu: 500m
          memory: 512Mi
    ingress:
      enabled: true
      ingressClassName: nginx
      hosts:
        - alertmanager-spike.labgrid.net
      paths:
        - /
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-production
        nginx.ingress.kubernetes.io/backend-protocol: HTTP
        nginx.ingress.kubernetes.io/force-ssl-redirect: "true"

  prometheusOperator:
    resources:
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        cpu: 500m
        memory: 512Mi

  # Set true only after monitoring-system is removed (host port 9100 conflicts otherwise).
  prometheus-node-exporter:
    enabled: false

grafana:
  replicas: 1
  admin:
    existingSecret: labgrid-monitoring-credentials
    userKey: grafana-admin-user
    passwordKey: grafana-admin-password

  persistence:
    enabled: true
    type: pvc
    storageClassName: synology-iscsi-delete-wffc
    accessModes:
      - ReadWriteOnce
    size: 10Gi

  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 512Mi

  ingress:
    enabled: true
    ingressClassName: nginx
    hosts:
      - grafana-spike.labgrid.net
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-production
      nginx.ingress.kubernetes.io/force-ssl-redirect: "true"

  sidecar:
    dashboards:
      enabled: true
      label: grafana_dashboard
      labelValue: "1"
      searchNamespace: ALL
    datasources:
      enabled: false

  datasources:
    datasources.yaml:
      apiVersion: 1
      datasources:
        - name: Prometheus
          type: prometheus
          uid: prometheus
          access: proxy
          isDefault: true
          url: http://kps-spike-prometheus.monitoring-spike.svc.cluster.local:9090
        - name: Loki
          type: loki
          uid: loki
          access: proxy
          url: http://monitoring-spike-loki-gateway.monitoring-spike.svc.cluster.local
          jsonData:
            httpHeaderName1: X-Scope-OrgID
          secureJsonData:
            httpHeaderValue1: fake
        - name: Tempo
          type: tempo
          uid: tempo
          access: proxy
          url: http://monitoring-spike-tempo.monitoring-spike.svc.cluster.local:3200
          jsonData:
            httpMethod: GET
            tracesToLogs:
              datasourceUid: loki
              filterByTraceID: true
              filterBySpanID: false
            serviceMap:
              datasourceUid: prometheus
            nodeGraph:
              enabled: true

loki:
  deploymentMode: SingleBinary

  singleBinary:
    replicas: 1
    resources:
      requests:
        cpu: 200m
        memory: 512Mi
      limits:
        cpu: 2000m
        memory: 2Gi
    persistence:
      enabled: true
      storageClass: synology-iscsi-delete-wffc
      accessModes:
        - ReadWriteOnce
      size: 50Gi

  write:
    replicas: 0
  read:
    replicas: 0
  backend:
    replicas: 0

  chunksCache:
    enabled: false
  resultsCache:
    enabled: false

  test:
    enabled: false

  loki:
    auth_enabled: false
    commonConfig:
      replication_factor: 1
    limits_config:
      retention_period: 744h
    storage:
      type: filesystem
      filesystem:
        chunks_directory: /var/loki/chunks
        rules_directory: /var/loki/rules
    schemaConfig:
      configs:
        - from: "2024-04-01"
          store: tsdb
          object_store: filesystem
          schema: v13
          index:
            prefix: index_
            period: 24h

tempo:
  fullnameOverride: monitoring-spike-tempo

  tempo:
    retention: 336h
    resources:
      requests:
        cpu: 200m
        memory: 512Mi
      limits:
        cpu: 2000m
        memory: 2Gi

  persistence:
    enabled: true
    storageClassName: synology-iscsi-delete-wffc
    accessModes:
      - ReadWriteOnce
    size: 30Gi

  serviceMonitor:
    enabled: true
    additionalLabels:
      release: monitoring-spike

alloy:
  enabled: true

  controller:
    type: daemonset

  alloy:
    configMap:
      create: false
      name: monitoring-spike-alloy
      key: config.alloy

  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 1Gi

  serviceMonitor:
    enabled: true
    additionalLabels:
      release: monitoring-spike
