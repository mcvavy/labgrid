{{- if .Values.postgresCluster.externalSecretBackupCredentials }}
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ .Values.postgresCluster.externalSecretBackupCredentials.name }}
  namespace: {{ .Release.Namespace | default .Values.namespaceOverride }}
  labels:
    app.kubernetes.io/component: {{ .Values.postgresCluster.externalSecretBackupCredentials.name | quote }}
    app.kubernetes.io/name: {{ .Chart.Name | quote }}
    app.kubernetes.io/instance: {{ .Release.Name | quote }}
spec:
  refreshInterval: {{ .Values.postgresCluster.externalSecretBackupCredentials.refreshInterval }}
  secretStoreRef:
    name: {{ .Values.postgresCluster.externalSecretBackupCredentials.secretStoreRef.name }}
    kind: {{ .Values.postgresCluster.externalSecretBackupCredentials.secretStoreRef.kind }}
  data:
{{- range $item := .Values.postgresCluster.externalSecretBackupCredentials.data }}
  - secretKey: {{ $item.secretKey }}
    remoteRef:
      key: {{ $item.remoteRef.key }}
{{- end }}
{{- end }}
